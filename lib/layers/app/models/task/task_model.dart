class TaskModel {
  String title;
  String description;
  DateTime expirationDate;

  TaskModel({
    required this.title,
    required this.description,
    required this.expirationDate,
  });
}
